# Visual brief — CAFE

Create a clean white-background research figure about the actual coupled agent/feedback learning loop. Show one shared-parameter model in two roles: search agent and critic. In the trajectory, the agent can issue request_feedback; the critic returns a correction that is appended before search/reasoning resumes. Around that loop, show three learning components: Comparative Feedback Estimate shaping the request action, feedback-aware advantage shaping around the intervention, and offline RDPO training the critic from matched successful/failed on-policy prefixes. Then show alternating online-agent and offline-critic updates. Include the same-family result 49.7 EM / 58.0 F1 (SFT+GRPO) → 52.5 / 60.7 (CAFE). Explicitly mark that a matched 500-online-step schedule study shows update cadence is a confound and that total feedback/search/token/latency cost is not fully matched. Do not imply the entire gain is caused by co-evolution alone.
