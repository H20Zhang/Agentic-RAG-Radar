# Visual grounding brief — Is Grep All You Need? (2605.15184)

**Visual question:** How can the same retriever/model look strong or weak solely because the harness or evidence-delivery path changes?

**Grounded mechanism:** A small factorial map with retrieval mode (`grep` vs `vector`) crossing harness/delivery (`inline` vs `programmatic`). Show ranking reversal rather than one universally superior method.

**Research delta:** Harness and tool-result delivery are causal experimental factors in agentic retrieval evaluation.

**Compared with:** Retriever-only comparisons that leave prompt/tool/result rendering implicit.

**Do not imply:** Do not generalize LongMemEval's lexical advantage to all domains; do not imply one harness mechanism was isolated; do not put benchmark numbers inside the image.

**Grounding status:** Full paper checked 2026-08-14.

**Preferred grammar:** Evidence/confound map. Use short labels: `grep`, `vector`, `harness`, `inline`, `file`, `ranking flips`.
