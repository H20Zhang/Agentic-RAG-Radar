# Visual brief — EviGraph

Create a clean white-background research figure, not a dashboard. Show the actual inference loop: executor sees question + constraints + rendered graph + query log + budget; Search returns pages to a frozen verifier; verifier emits source-anchored verbatim evidence items with constraint tag and support/conflict polarity; the shared policy in evidence-proposal role requests Add/Support; a deterministic validator checks exact provenance/structure and commits; the graph exposes positive support, conflict, and MISSING constraints back to the executor. Mark three separable failure points: retrieval failure, recording failure, decision/stopping failure. Include the controlled evidence 26.9% dual-role no-RL → 35.9% dual-role+RL on BrowseComp-Plus, and visually separate the 2.7% monolithic baseline as a package-level comparison. State that the validator checks structure, not semantic truth, and that the frozen verifier is privileged semantic supervision. Do not invent code, project URLs, benchmarks, or resource numbers.
