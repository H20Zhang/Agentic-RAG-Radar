name: Agentic RAG staging transform

on:
  push:
    branches: [research-radar-reading-v1]

permissions:
  contents: write

jobs:
  stage:
    if: github.event.head_commit.message == 'Prepare Agentic RAG 2026-09-04 staging transform'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research-radar-reading-v1
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install validator dependencies
        run: pip install 'jsonschema[format]>=4.23,<5' 'pillow>=11,<12'
      - name: Apply accepted CoBRA transaction
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import json, re

          CUTOFF = '2026-09-04T01:53:14Z'
          FIRST_SEEN_AT = '2026-09-04T01:44:40Z'
          rid = '2609.00967'

          record = {
            'id': 'arxiv:2609.00967',
            'arxiv_id': rid,
            'title': 'CoBRA: Learning Tool-Use Boundaries via Counterfactual Margins',
            'authors': ['Wenhao Zou', 'Xianglong Liu', 'Wendong Bi', 'Hanjie Wang', 'Simin Zhao', 'Gong Zhi'],
            'first_seen': '2026-09-04',
            'published': '2026-09-01',
            'published_at': '2026-09-01T09:24:11Z',
            'first_seen_at': FIRST_SEEN_AT,
            'radar_published_at': CUTOFF,
            'time_provenance': 'native_v2',
            'map_delta': 'early_signal',
            'direction_keys': ['counterfactual-retrieval-routing'],
            'updated': '2026-09-04',
            'venue': 'EMNLP 2026',
            'urls': {
              'paper': 'https://arxiv.org/abs/2609.00967',
              'code': None,
              'project': None,
            },
            'primary_category': 'learning_optimization',
            'tags': ['selective_retrieval', 'tool_routing', 'counterfactual_margin', 'reinforcement_learning', 'retrieval_budgeting', 'causal_attribution'],
            'benchmarks': ['TriviaQA', 'HotpotQA', '2WikiMultiHopQA', 'Natural Questions', 'PopQA', 'Boundary-Eval'],
            'relevance': 0.98,
            'importance': 4,
            'visual_explainer': {
              'type': 'evidence_failure_map',
              'question': 'Should this query invoke retrieval, and can the routing decision be trained from paired internal/external utility rather than absolute difficulty?',
              'takeaway': 'Reference-split rollouts make route-level marginal utility an explicit learning signal, but discovery labels and realized resource differences still bound causal attribution.',
              'compared_with': 'the same warm-start policy with MARS-RL factors removed, plus prompted, heuristic, and search-RL routing baselines',
              'full_text_grounded': True,
              'renderer': 'gpt-image-gen',
              'status': 'pending',
              'master_image_path': None,
              'image_path': 'assets/visuals/2609.00967.webp',
              'prompt_path': 'assets/visuals/prompts/2609.00967.md',
              'artifact_path': 'papers/2609.00967.md',
            },
            'analysis': {
              'tldr': 'CoBRA turns selective retrieval into a route-level marginal-utility learning problem: for the same query, compare internal and retrieval-forced branches, then train the router from that branch gap rather than query difficulty or final reward alone.',
              'problem': 'Difficulty, uncertainty, or outcome-only reward does not identify whether retrieval itself is worth paying for on a particular query; a policy can over-call search, under-call it, or improve answers without learning the internal-versus-external boundary.',
              'core_idea': 'Construct internal and external branches, estimate per-query utility margins, use clear margins for cold-start SFT, then use MARS-RL reference-split rollouts with per-branch normalization and a symmetric branch-margin term on boundary-heavy queries.',
              'agent_loop': 'A Qwen3-4B agent receives one router-style prompt with a Wikipedia search tool, chooses direct answering or retrieval, may make up to four tool turns, and produces a final answer; MARS-RL forces paired internal/external first decisions from the same reference policy to estimate route-level advantage.',
              'retrieval_design': 'An E5-base dense retriever over about 21M passages from the 2018 Wikipedia dump returns top-3 passages per call; the online utility subtracts 0.10 times the number of tool calls from judged exact-match reward.',
              'compared_to': ['M_warm under the same tool schema before MARS-RL', 'MARS-RL component ablations removing reference-split rollouts, per-branch normalization, or branch margin', 'SKR-kNN in the same retrieval/evaluation environment', 'Search-R1-3B and prompted ReAct/Self-Ask baselines'],
              'evidence': 'Under warm initialization, full MARS reaches 0.5418 average jEM versus 0.5066 without reference-split rollouts, 0.4866 without per-branch normalization, and 0.4817 without the branch-margin term. Reference-split branches share the system prompt, tool schema, and reference policy except for the internal suppression versus external tool-call-prefix intervention.',
              'why_it_matters': 'The paper moves adaptivity placement from a heuristic question of when to search toward a counterfactual question: what is the marginal utility of external acquisition for this same query under an explicit route intervention?',
              'limitations': [
                'Counterfactual Boundary Discovery compares two separately rejection-sample-fine-tuned experts, so its initial margins mix route value with branch-specific policy competence; the later MARS reference-split intervention is cleaner.',
                'MARS component ablations do not match realized tool calls, and utility prices only call count with a fixed lambda=0.10 rather than matching tokens, latency, API cost, or retrieved-evidence volume.',
                'The main study uses one Qwen3-4B family and Wikipedia retrieval; the reported training pipeline also requires two expert SFTs, 777,404 paired rollouts, cold-start SFT, and about 100 GPU-hours for the MARS-RL stage alone.'
              ],
              'confidence': 'medium',
            },
            'provenance': {
              'discovered_via': ['arXiv cs.AI primary-source sweep', 'independent selective-retrieval/tool-routing sweep'],
              'full_text_checked': True,
              'code_link_verified': False,
              'last_reviewed': '2026-09-04',
            },
          }
          Path(f'data/papers/{rid}.json').write_text(json.dumps(record, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')

          en_note = r'''# CoBRA: Learning Tool-Use Boundaries via Counterfactual Margins

*Published 2026-09-01 · Learning & Optimization · Importance 4/5 · Full text reviewed · Confidence: medium*

[Paper](https://arxiv.org/abs/2609.00967)

> **TL;DR.** CoBRA reframes selective retrieval as a **same-query marginal-utility decision** rather than a difficulty or uncertainty heuristic. Its cleanest mechanism is MARS-RL: internal and retrieval-forced rollouts share the system prompt, tool schema, and reference policy, then the branch reward gap enters the policy advantage. That is stronger route-level supervision, but it is still not a budget-matched causal estimate of “retrieval content alone.”

| 30-second verdict | |
|---|---|
| **Why this paper matters** | It makes “should I retrieve?” an explicit intervention target: compare internal and external branches on the same query, not retrieved versus non-retrieved task populations. |
| **Strongest evidence** | With the same warm initialization, full MARS reaches **0.5418 Avg jEM**, versus **0.5066** without reference-split rollouts, **0.4866** without per-branch normalization, and **0.4817** without the branch-margin term. |
| **Biggest caveat** | The discovery margins come from two separately fine-tuned experts, while the MARS ablations change realized tool-call rates; token, latency, retrieved-byte, and dollar budgets are not matched. |

## Research question

For a particular query, is external retrieval worth its cost **relative to answering from the same model's internal knowledge**, and can that marginal value become the learning signal for routing?

## Mechanism

CoBRA uses Qwen3-4B with a Wikipedia search tool. Counterfactual Boundary Discovery first creates an internal expert trained on successful no-tool traces and an external expert trained on successful retrieval traces. Running both on the same large query pool yields a utility margin `external − internal`, where utility is judged exact match minus `0.10 × #Tool`. Clear internal-favored and external-favored cases train a warm router. MARS-RL then revisits boundary-heavy cases: for each query, reference-split rollouts suppress the first tool action on one branch and prefix a tool call on the other, while keeping the system prompt, tool schema, and reference policy shared. Per-branch normalization plus a symmetric branch-margin term injects the route difference into policy advantage.

## Research delta

The closest conceptual predecessors are selective-retrieval routers such as SKR and outcome-trained search agents. Their trigger can correlate with uncertainty, complexity, or final reward without directly estimating **whether this query benefits from retrieval**. CoBRA changes the optimization object from an absolute trigger score to a paired internal-versus-external utility margin. The important comparison is therefore not its heterogeneous headline leaderboard, but whether the route intervention survives matched-policy ablations.

## Evidence & attribution

| Claim | Evidence | Control | Reading |
|---|---|---|---|
| route-specific credit adds signal beyond outcome-only GRPO | warm-init Avg jEM: MARS **0.5418**; vanilla GRPO **0.2734** | same Qwen3-4B family and training setup | final reward alone collapses toward no-tool behavior in this setup |
| all three MARS factors matter in the reported package | no RS **0.5066**, no PBN **0.4866**, no BM **0.4817**, full **0.5418** Avg jEM | same warm initialization | paired branches, branch-relative normalization, and margin credit are jointly useful |
| routing gains are not simply “search more” | Boundary-Eval ambiguous subset: CoBRA **0.9658 jEM / 1.015 calls** vs Search-R1-3B **0.9044 / 1.241** | same benchmark, but different model/system | useful system-level evidence, not clean component attribution |
| a same-environment heuristic baseline remains weaker | OOD SKR-kNN **0.4396 jEM / 1.4025 calls** vs CoBRA **0.5416 / 0.9970** | same retrieval/evaluation environment | counterfactual training beats this kNN router under the paper's setup |

## Resource accounting

The online metric prices only search-call count: `Util = jEM − 0.10 × #Tool`. That is a useful explicit trade-off, not a complete operating-cost model. The offline pipeline is substantial: two expert SFTs take about **4 GPU-hours each**, paired counterfactual generation about **35 GPU-hours**, warm-router SFT about **4 GPU-hours**, and one MARS-RL run about **100 GPU-hours** on the reported H20 setup; S2 alone generates **777,404** paired rollouts. Retrieval uses top-3 passages and up to four tool turns. Token, wall-clock, API-dollar, and evidence-volume matching are still absent from the main comparisons.

## What remains unproven

The first “counterfactual” partition is not a pure do-intervention: the internal and external experts were trained on different rejection-sampled success traces, so their margin contains both route value and branch competence. MARS-RL fixes much of the interface problem by branching the same reference policy, but its ablations still produce different call counts. A stronger experiment would fix one policy checkpoint, force internal/external branches on a pre-specified query population, match retrieval result volume and realized token/latency budgets, then randomize returned evidence or its visibility to separate **invocation value** from **evidence value**.

## Where it fits

`query → internal/external route intervention → evidence acquisition → answer → route-level marginal credit`.

`early_signal`: **counterfactual retrieval routing** is a promising adaptivity coordinate, but one Qwen3/Wikipedia study does not change the durable Field Map.

## Related reading

[Skill Following](2609.00549.md) · [CAS](2608.20771.md) · [SAGE](2608.08237.md) · [SearchMaster](2608.01822.md) · [Research Map](../categories/README.en.md)
'''
          Path(f'papers/{rid}.md').write_text(en_note, encoding='utf-8')

          zh_note = r'''# CoBRA: Learning Tool-Use Boundaries via Counterfactual Margins

*发表于 2026-09-01 · 学习与优化 · 重要性 4/5 · 已阅读全文 · 置信度：中*

[论文](https://arxiv.org/abs/2609.00967)

> **TL;DR。** CoBRA 把选择性检索从“这个问题看起来难不难”改写成**同一查询上调用外部检索的边际效用**。其中最干净的设计是 MARS-RL：内部作答分支与强制检索分支共享 system prompt、工具 schema 和 reference policy，再把两个分支的奖励差写进 policy advantage。它强化了路由层监督，但仍不能当成“检索内容本身”的等预算因果效应。

| 30 秒判断 | |
|---|---|
| **为什么重要** | 它把“要不要检索”变成显式干预对象：在同一查询上比较内部作答与外部检索，而不是比较两组由策略自己挑出来的任务。 |
| **最强证据** | 在相同 warm initialization 下，完整 MARS 的 **Avg jEM=0.5418**；去掉 reference-split rollout 后为 **0.5066**，去掉 per-branch normalization 为 **0.4866**，去掉 branch-margin term 为 **0.4817**。 |
| **最大限制** | 初始 margin 来自两个分别微调的 expert；MARS 消融又改变了实际工具调用量，因此 token、延迟、证据量和美元成本仍未配平。 |

## Problem

对某个具体查询，外部检索相对于同一模型依赖内部知识作答，究竟值不值得付出成本？这个**边际价值**能否直接成为检索路由的学习信号？

## Mechanism

CoBRA 以 Qwen3-4B 和 Wikipedia 搜索工具为主体。Counterfactual Boundary Discovery 先分别训练 internal expert 与 external expert：前者只保留成功的无工具轨迹，后者只保留成功的检索轨迹。两者在同一大规模查询池上运行，得到 `external − internal` 的效用差，其中效用定义为 judged exact match 减去 `0.10 × #Tool`。清晰的 internal-favored / external-favored 样本用于训练 warm router。随后 MARS-RL 专门处理边界样本：同一查询从相同 reference policy 分成两条 rollout，一条在第一步禁止工具调用，另一条预置 tool call；system prompt 与工具 schema 保持一致。再通过分支内归一化和对称 branch-margin term，把路由差异写入策略优势。

## Closest comparison

最近的比较对象是 SKR 一类选择性检索路由器和依赖最终结果奖励训练的 search agent。它们可以根据不确定性、复杂度或最终 reward 学到触发规则，却不直接估计**这个查询是否因为检索而受益**。CoBRA 真正改变的是优化对象：从绝对触发分数变成同查询的 internal-versus-external utility margin。因此最重要的证据不是异构 leaderboard 的 headline，而是 route intervention 在 matched-policy 消融中是否成立。

## Decisive evidence

| 主张 | 证据 | 最近控制 | 判断 |
|---|---|---|---|
| 路由专属 credit 比 outcome-only GRPO 多出有效信号 | warm-init Avg jEM：MARS **0.5418**，Vanilla GRPO **0.2734** | 同一 Qwen3-4B family 与训练框架 | 该设置下仅靠最终奖励会塌向几乎不调用工具 |
| MARS 三个因素在当前 package 中都贡献增量 | 去 RS **0.5066**、去 PBN **0.4866**、去 BM **0.4817**、完整 **0.5418** | 相同 warm initialization | paired branch、分支归一化与 margin credit 的组合有用 |
| 结果不是简单依赖“搜得更多” | Boundary-Eval ambiguous：CoBRA **0.9658 jEM / 1.015 calls**，Search-R1-3B **0.9044 / 1.241** | 同一 benchmark，但模型和系统不同 | 只能作为系统级证据，不能做组件归因 |
| 同检索环境的 heuristic router 仍较弱 | OOD SKR-kNN **0.4396 jEM / 1.4025 calls**，CoBRA **0.5416 / 0.9970** | 同一 retrieval / evaluation environment | 在论文设置下，counterfactual training 优于该 kNN router |

## Cost

在线指标只给搜索调用次数定价：`Util = jEM − 0.10 × #Tool`。这比只报准确率更好，但仍不是完整成本。离线训练相当重：两个 expert SFT 各约 **4 GPU-hours**，paired counterfactual generation 约 **35 GPU-hours**，warm-router SFT 约 **4 GPU-hours**，单次 MARS-RL 约 **100 GPU-hours**；S2 本身生成 **777,404** 条 paired rollouts。检索每次返回 top-3 passage，每条 trajectory 最多四个 tool turn。主实验仍缺 token、wall-clock、API 美元成本与 evidence volume 的匹配。

## What remains unproven

第一阶段所谓“counterfactual”不是纯粹的 do-intervention：internal 与 external expert 分别由不同的成功轨迹做 rejection-sampling SFT，因此 margin 同时包含**路由价值**与**分支策略能力**。MARS-RL 用同一 reference policy 分支，解决了大部分接口混杂，但各消融的实际调用次数仍不同。下一步更强的实验应固定同一 policy checkpoint，在预先确定的查询总体上强制 internal/external route，配平返回证据量与实际 token/latency，再分别随机化 returned evidence 和 visibility，才能拆出 **invocation value** 与 **evidence value**。

## Field-map consequence

`查询 → internal/external 路由干预 → 证据获取 → 作答 → 路由层边际 credit`。

这是 `early_signal`：**counterfactual retrieval routing** 是值得单独研究的 adaptivity coordinate；单篇 Qwen3/Wikipedia 工作不改 durable Field Map。

## Related reading

[Skill Following](2609.00549.zh.md) · [CAS](2608.20771.zh.md) · [SAGE](2608.08237.md) · [SearchMaster](2608.01822.md) · [Research Map](../categories/README.md)
'''
          Path(f'papers/{rid}.zh.md').write_text(zh_note, encoding='utf-8')

          visual = '''# Visual brief — CoBRA\n\nCreate a clean research figure about selective retrieval as a counterfactual routing decision. Show one query branching into internal/no-search and external/search paths under the same reference policy, system prompt, and tool schema; the branch reward gap becomes route-level credit. Include the warm-init MARS ablation: full 0.5418 Avg jEM, no-RS 0.5066, no-PBN 0.4866, no-BM 0.4817. Mark the causal boundary explicitly: initial discovery uses two separately SFTed experts, realized tool calls differ across ablations, and the paper prices call count rather than matching tokens/latency/evidence volume/dollars. Do not imply that retrieval content alone caused the gain.\n'''
          Path(f'assets/visuals/prompts/{rid}.md').write_text(visual, encoding='utf-8')

          # Category projection.
          p = Path('categories/learning-optimization.md')
          s = p.read_text(encoding='utf-8')
          marker = '## Current papers\n\n'
          entry = '''\n### [CoBRA](../papers/2609.00967.md) — ★★★★☆\n\n**Design point:** train selective retrieval from a same-query internal-versus-external utility margin, then inject that route-level margin through reference-split RL. **Boundary:** the discovery experts are separately fine-tuned and MARS ablations do not match realized tool calls, tokens, latency, or evidence volume.\n'''
          assert marker in s and '### [CoBRA]' not in s
          p.write_text(s.replace(marker, marker + entry, 1), encoding='utf-8')

          # Library research-line projection: evaluation of actual use -> training route value.
          for name in ['library/README.md', 'library/README.en.md']:
            p = Path(name); s = p.read_text(encoding='utf-8')
            if name.endswith('README.md'):
              old = '[Skill Following](../papers/2609.00549.zh.md) → [VAKRA](../papers/2608.12282.zh.md)'
              new = '[Skill Following](../papers/2609.00549.zh.md) → [CoBRA](../papers/2609.00967.zh.md) → [VAKRA](../papers/2608.12282.zh.md)'
              s = s.replace('Skill Following 又拆出一层：选择在哪些任务上检索，不等于检索后对同一任务产生了实际效用。只有这些路径被配平，排行榜增益才可能从系统层证据收缩为组件证据。', 'Skill Following 先拆开“选择检索”与“同任务实际效用”；CoBRA 再把同查询 internal/external 的边际效用用于训练路由。只有接口、分支能力与实际资源被配平，排行榜增益才可能从系统层证据收缩为组件证据。')
            else:
              old = '[Skill Following](../papers/2609.00549.md) → [VAKRA](../papers/2608.12282.md)'
              new = '[Skill Following](../papers/2609.00549.md) → [CoBRA](../papers/2609.00967.md) → [VAKRA](../papers/2608.12282.md)'
              s = s.replace('Skill Following adds another split: selective retrieval coverage is not realized same-task utility. Leaderboard gains remain system-level evidence until these paths are matched.', 'Skill Following separates selective retrieval from realized same-task utility; CoBRA then trains routing from an internal-versus-external margin on the same query. Leaderboard gains remain system-level evidence until interface, branch competence, and realized resources are matched.')
            assert old in s
            p.write_text(s.replace(old, new, 1), encoding='utf-8')

          cn_card = r'''<a id="entry-2609.00967"></a>
          <details><summary>2026-09-04 · CoBRA · Adaptivity placement → counterfactual retrieval routing <!-- timefirst:area=counterfactual-retrieval-routing --> — 把“要不要检索”的学习目标从 query difficulty / final reward 改成同一查询 internal 与 external route 的边际效用。 <!-- timefirst:delta=counterfactual-retrieval-routing --></summary>

          **问题。** 对同一查询，怎样直接学习“调用外部检索是否比依赖内部知识更值得”，而不是用难度、不确定性或最终奖励做代理？ <!-- timefirst:question=counterfactual-route-utility -->

          **证据。** 相同 warm initialization 下，完整 MARS 的 Avg jEM 为 **0.5418**；去掉 reference-split rollout 为 **0.5066**，去掉 per-branch normalization 为 **0.4866**，去掉 branch-margin term 为 **0.4817**。RS 两分支共享 system prompt、tool schema 与 reference policy。 <!-- timefirst:evidence=counterfactual-routing~warm-init-mars-ablation -->

          **限制。** 初始 margin 来自两个分别 SFT 的 expert；MARS 消融的实际工具调用量也不同，`Util` 只按 `0.10 × #Tool` 定价，没有匹配 token、latency、证据量或美元成本。 <!-- timefirst:caveat=counterfactual-routing~branch-competence-and-budget -->

          **地图。** `early_signal`：counterfactual retrieval routing 是新的 adaptivity coordinate；单篇 Qwen3/Wikipedia 研究不改 durable Field Map。

          **链接。** [CoBRA: Learning Tool-Use Boundaries via Counterfactual Margins](https://arxiv.org/abs/2609.00967) · [英文深读](papers/2609.00967.md) · [中文深读](papers/2609.00967.zh.md)

          </details>

          '''.replace('          ', '')

          en_card = r'''<a id="entry-2609.00967"></a>
          <details><summary>2026-09-04 · CoBRA · Adaptivity placement → counterfactual retrieval routing <!-- timefirst:area=counterfactual-retrieval-routing --> — Replaces query-difficulty/final-reward routing targets with the same-query marginal utility of internal versus external routes. <!-- timefirst:delta=counterfactual-retrieval-routing --></summary>

          **Question.** For the same query, can a policy learn whether external retrieval is worth more than internal knowledge rather than use difficulty, uncertainty, or final reward as a proxy? <!-- timefirst:question=counterfactual-route-utility -->

          **Evidence.** Under the same warm initialization, full MARS reaches **0.5418 Avg jEM** versus **0.5066** without reference-split rollouts, **0.4866** without per-branch normalization, and **0.4817** without the branch-margin term. RS branches share the system prompt, tool schema, and reference policy. <!-- timefirst:evidence=counterfactual-routing~warm-init-mars-ablation -->

          **Caveat.** Initial margins come from two separately SFTed experts; MARS ablations also change realized tool-call rates, while `Util` prices only `0.10 × #Tool` rather than matching tokens, latency, evidence volume, or dollars. <!-- timefirst:caveat=counterfactual-routing~branch-competence-and-budget -->

          **Map.** `early_signal`: counterfactual retrieval routing is a new adaptivity coordinate; one Qwen3/Wikipedia study does not change the durable Field Map.

          **Links.** [CoBRA: Learning Tool-Use Boundaries via Counterfactual Margins](https://arxiv.org/abs/2609.00967) · [English deep note](papers/2609.00967.md) · [Chinese deep note](papers/2609.00967.zh.md)

          </details>

          '''.replace('          ', '')

          cn_dir = r'''- **`new_signal` · Counterfactual retrieval routing · 路由目标从 query difficulty 转向同查询的 external-minus-internal 边际效用。** <!-- timefirst:direction key="counterfactual-retrieval-routing" state="new_signal" supports="2609.00967" confidence="medium" implication="intervene-on-route-under-one-policy-and-match-realized-resources" timing="radar_published_at" synthesized="2026-09-04T01:53:14Z" prior="none" -->
            支撑：[CoBRA](#entry-2609.00967)；置信度：**medium**；时间依据：`radar_published_at`；精确合成时间：`2026-09-04T01:53:14Z`（UTC）；研究设计含义（intervene-on-route-under-one-policy-and-match-realized-resources）：固定同一 policy、prompt、tool schema 与查询总体，强制 internal/external route，并匹配 returned evidence、token、调用与 latency 后再估计边际效用；先验地图证据：`none`。

          '''.replace('          ', '').replace('            ', '  ')

          en_dir = r'''- **`new_signal` · Counterfactual retrieval routing · routing moves from query difficulty toward same-query external-minus-internal marginal utility.** <!-- timefirst:direction key="counterfactual-retrieval-routing" state="new_signal" supports="2609.00967" confidence="medium" implication="intervene-on-route-under-one-policy-and-match-realized-resources" timing="radar_published_at" synthesized="2026-09-04T01:53:14Z" prior="none" -->
            Supports: [CoBRA](#entry-2609.00967); confidence: **medium**; timing basis: `radar_published_at`; Exact synthesis time: `2026-09-04T01:53:14Z` (UTC); Research-design implication (intervene-on-route-under-one-policy-and-match-realized-resources): hold one policy, prompt, tool schema, and query population fixed; force internal/external routes and match returned evidence, tokens, calls, and latency before estimating marginal utility; prior map evidence: `none`.

          '''.replace('          ', '').replace('            ', '  ')

          # README Chinese projection.
          p = Path('README.md'); s = p.read_text(encoding='utf-8')
          assert 'entry-2609.00967' not in s
          s = re.sub(r'\*\*状态：\*\* 最后更新：\*\*2026-09-03\*\* · 最后合成：\*\*2026-09-03T01:27:38Z（UTC）\*\*', f'**状态：** 最后更新：**2026-09-04** · 最后合成：**{CUTOFF}（UTC）**', s, count=1)
          s = s.replace('<a id="entry-2609.00549"></a>\n', cn_card + '<a id="entry-2609.00549"></a>\n', 1)
          # Exact shared synthesis timestamp for every currently visible direction.
          s = s.replace('2026-09-03T01:27:38Z', CUTOFF)
          # Rebuild exact 7-day window; AWM accepted on Aug 28 falls out on Sep 4.
          m = re.search(r'### 过去 7 天 · .*?\n\n(.*?)<a id="last-30-days"></a>', s, flags=re.S)
          assert m
          existing7 = m.group(1)
          def block_for(key):
            mm = re.search(r'- \*\*`new_signal` · .*?timefirst:direction key="'+re.escape(key)+r'".*?(?=\n- \*\*|\Z)', existing7, flags=re.S)
            assert mm, key
            return mm.group(0).rstrip() + '\n\n'
          seven = cn_dir + block_for('retrieval-invoked-actual-use') + block_for('interaction-conditioned-retrieval') + block_for('multimodal-evidence-persistence') + block_for('proactive-context-management')
          replacement = '### 过去 7 天 · 2026-08-29—2026-09-04\n\n' + seven + '<a id="last-30-days"></a>'
          s = s[:m.start()] + replacement + s[m.end():]
          old30 = '### 过去 30 天 · 2026-08-05—2026-09-03\n\n'
          assert old30 in s
          s = s.replace(old30, '### 过去 30 天 · 2026-08-06—2026-09-04\n\n' + cn_dir, 1)
          old_link = '[Skill Following](papers/2609.00549.zh.md) → [VAKRA](papers/2608.12282.md)'
          if old_link in s:
            s = s.replace(old_link, '[Skill Following](papers/2609.00549.zh.md) → [CoBRA](papers/2609.00967.zh.md) → [VAKRA](papers/2608.12282.md)', 1)
          p.write_text(s, encoding='utf-8')

          # README English projection.
          p = Path('README.en.md'); s = p.read_text(encoding='utf-8')
          assert 'entry-2609.00967' not in s
          s = re.sub(r'\*\*Status:\*\* Last updated: \*\*2026-09-03\*\* · Last synthesized: \*\*2026-09-03T01:27:38Z \(UTC\)\*\*', f'**Status:** Last updated: **2026-09-04** · Last synthesized: **{CUTOFF} (UTC)**', s, count=1)
          s = s.replace('<a id="entry-2609.00549"></a>\n', en_card + '<a id="entry-2609.00549"></a>\n', 1)
          s = s.replace('2026-09-03T01:27:38Z', CUTOFF)
          m = re.search(r'### Last 7 days · .*?\n\n(.*?)<a id="last-30-days"></a>', s, flags=re.S)
          assert m
          existing7 = m.group(1)
          def block_for_en(key):
            mm = re.search(r'- \*\*`new_signal` · .*?timefirst:direction key="'+re.escape(key)+r'".*?(?=\n- \*\*|\Z)', existing7, flags=re.S)
            assert mm, key
            return mm.group(0).rstrip() + '\n\n'
          seven = en_dir + block_for_en('retrieval-invoked-actual-use') + block_for_en('interaction-conditioned-retrieval') + block_for_en('multimodal-evidence-persistence') + block_for_en('proactive-context-management')
          replacement = '### Last 7 days · 2026-08-29—2026-09-04\n\n' + seven + '<a id="last-30-days"></a>'
          s = s[:m.start()] + replacement + s[m.end():]
          old30 = '### Last 30 days · 2026-08-05—2026-09-03\n\n'
          assert old30 in s
          s = s.replace(old30, '### Last 30 days · 2026-08-06—2026-09-04\n\n' + en_dir, 1)
          old_link = '[Skill Following](papers/2609.00549.md) → [VAKRA](papers/2608.12282.md)'
          if old_link in s:
            s = s.replace(old_link, '[Skill Following](papers/2609.00549.md) → [CoBRA](papers/2609.00967.md) → [VAKRA](papers/2608.12282.md)', 1)
          p.write_text(s, encoding='utf-8')
          PY
      - name: Build generated index
        run: python scripts/build_paper_index.py
      - name: Validate transaction
        run: |
          python -m unittest discover -s tests -v
          python scripts/build_paper_index.py --check
          python scripts/validate.py
          python scripts/validate_reading.py
          python scripts/validate_public.py
          git diff --check
      - name: Commit validated staging tree
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/workflows/agentic-rag-stage.yml
          git add -A
          git commit -m 'Stage Agentic RAG update 2026-09-04'
          git push origin HEAD:research-radar-reading-v1
